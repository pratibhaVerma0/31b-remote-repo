Month’s first ETL run:

1. If column values are not changed from last month à add a new row with latest corresponding timestamp.

2. If values are changed à add a new row with latest corresponding timestamp.

3. If new country/primary key is added, add a new row with latest corresponding timestamp.

Month’s rest of the ETL runs:

1. If column values are not changed à do nothing with no change on Timestamp.

2. If column values are changed à update the row with new timeline and corresponding latest timestamp.

3. Country/primary key is added à add a new row with corresponding latest timestamp.
